name: Build IPA

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest
    timeout-minutes: 30

    steps:
      - name: 拉取代码
        uses: actions/checkout@v4

      - name: 设置 Xcode
        run: |
          sudo xcode-select -switch /Applications/Xcode.app/Contents/Developer
          xcodebuild -version

      - name: 创建源码
        run: |
          mkdir -p CloudFlareScan
          mkdir -p CloudFlareScan.xcodeproj

          cat > CloudFlareScan/main.m << 'EOF'
          #import <UIKit/UIKit.h>

          // ===== IP数据 =====
          @interface IPInfo : NSObject
          @property (nonatomic, strong) NSString *ip;
          @property (nonatomic, assign) NSInteger port;
          @property (nonatomic, assign) double delay;
          @property (nonatomic, strong) NSString *country;
          @property (nonatomic, strong) NSString *colo;
          @end
          @implementation IPInfo
          @end

          // ===== 主界面 =====
          @interface ViewController : UIViewController <UITableViewDelegate, UITableViewDataSource>
          @property (nonatomic, strong) NSMutableArray *results;
          @property (nonatomic, strong) UITextView *inputTV;
          @property (nonatomic, strong) UITableView *tableView;
          @property (nonatomic, strong) UILabel *statusLabel;
          @property (nonatomic, strong) UILabel *countLabel;
          @property (nonatomic, strong) UIButton *scanBtn;
          @property (nonatomic, strong) UIButton *stopBtn;
          @property (nonatomic, strong) UIButton *importBtn;
          @property (nonatomic, strong) UIButton *copyBtn;
          @property (nonatomic, strong) UIButton *clearBtn;
          @property (nonatomic, assign) BOOL isScanning;
          @end

          @implementation ViewController

          - (void)viewDidLoad {
              [super viewDidLoad];
              self.view.backgroundColor = [UIColor colorWithRed:0.06 green:0.08 blue:0.15 alpha:1];
              self.title = @"🌐 优选IP筛选";
              self.results = [NSMutableArray array];
              [self setupUI];
              [self detectISP];
          }

          - (void)setupUI {
              UIScrollView *scroll = [[UIScrollView alloc] init];
              scroll.translatesAutoresizingMaskIntoConstraints = NO;
              [self.view addSubview:scroll];

              [NSLayoutConstraint activateConstraints:@[
                  [scroll.topAnchor constraintEqualToAnchor:self.view.safeAreaLayoutGuide.topAnchor],
                  [scroll.leadingAnchor constraintEqualToAnchor:self.view.leadingAnchor],
                  [scroll.trailingAnchor constraintEqualToAnchor:self.view.trailingAnchor],
                  [scroll.bottomAnchor constraintEqualToAnchor:self.view.safeAreaLayoutGuide.bottomAnchor]
              ]];

              UIView *content = [[UIView alloc] init];
              content.translatesAutoresizingMaskIntoConstraints = NO;
              [scroll addSubview:content];

              [NSLayoutConstraint activateConstraints:@[
                  [content.topAnchor constraintEqualToAnchor:scroll.topAnchor],
                  [content.leadingAnchor constraintEqualToAnchor:scroll.leadingAnchor],
                  [content.trailingAnchor constraintEqualToAnchor:scroll.trailingAnchor],
                  [content.bottomAnchor constraintEqualToAnchor:scroll.bottomAnchor],
                  [content.widthAnchor constraintEqualToAnchor:scroll.widthAnchor]
              ]];

              CGFloat w = self.view.bounds.size.width - 24;
              CGFloat y = 12;

              // ---- 状态 ----
              self.statusLabel = [[UILabel alloc] init];
              self.statusLabel.text = @"就绪";
              self.statusLabel.textColor = [UIColor grayColor];
              self.statusLabel.font = [UIFont systemFontOfSize:12];
              self.statusLabel.frame = CGRectMake(12, y, 200, 20);
              [content addSubview:self.statusLabel];

              UILabel *ispLabel = [[UILabel alloc] init];
              ispLabel.text = @"📍 检测中...";
              ispLabel.textColor = [UIColor colorWithRed:0 green:0.78 blue:0.98 alpha:1];
              ispLabel.font = [UIFont systemFontOfSize:12];
              ispLabel.textAlignment = NSTextAlignmentRight;
              ispLabel.tag = 100;
              ispLabel.frame = CGRectMake(w - 180, y, 180, 20);
              [content addSubview:ispLabel];

              y += 28;

              // ---- 统计 ----
              self.countLabel = [[UILabel alloc] init];
              self.countLabel.text = @"总数: 0  可用: 0";
              self.countLabel.textColor = [UIColor whiteColor];
              self.countLabel.font = [UIFont systemFontOfSize:14];
              self.countLabel.frame = CGRectMake(12, y, w, 24);
              [content addSubview:self.countLabel];

              y += 32;

              // ---- 输入 ----
              UIView *inputBg = [[UIView alloc] init];
              inputBg.backgroundColor = [UIColor colorWithRed:0.10 green:0.13 blue:0.22 alpha:1];
              inputBg.layer.cornerRadius = 8;
              inputBg.frame = CGRectMake(12, y, w, 100);
              [content addSubview:inputBg];

              UILabel *inputLabel = [[UILabel alloc] init];
              inputLabel.text = @"📂 导入IP (每行一个)";
              inputLabel.textColor = [UIColor grayColor];
              inputLabel.font = [UIFont systemFontOfSize:11];
              inputLabel.frame = CGRectMake(12, 6, 200, 20);
              [inputBg addSubview:inputLabel];

              self.inputTV = [[UITextView alloc] init];
              self.inputTV.backgroundColor = [UIColor colorWithRed:0.06 green:0.08 blue:0.15 alpha:1];
              self.inputTV.textColor = [UIColor whiteColor];
              self.inputTV.font = [UIFont systemFontOfSize:12];
              self.inputTV.layer.cornerRadius = 4;
              self.inputTV.text = @"1.1.1.1\n8.8.8.8\n9.9.9.9";
              self.inputTV.frame = CGRectMake(8, 30, inputBg.bounds.size.width - 16, 62);
              [inputBg addSubview:self.inputTV];

              y += 108;

              // ---- 按钮 ----
              UIView *btnBg = [[UIView alloc] init];
              btnBg.backgroundColor = [UIColor colorWithRed:0.10 green:0.13 blue:0.22 alpha:1];
              btnBg.layer.cornerRadius = 8;
              btnBg.frame = CGRectMake(12, y, w, 46);
              [content addSubview:btnBg];

              self.scanBtn = [UIButton buttonWithType:UIButtonTypeSystem];
              [self.scanBtn setTitle:@"▶ 扫描" forState:UIControlStateNormal];
              [self.scanBtn setTitleColor:[UIColor whiteColor] forState:UIControlStateNormal];
              self.scanBtn.backgroundColor = [UIColor colorWithRed:0.2 green:0.5 blue:0.9 alpha:1];
              self.scanBtn.layer.cornerRadius = 6;
              self.scanBtn.frame = CGRectMake(6, 6, 60, 34);
              [self.scanBtn addTarget:self action:@selector(startScan) forControlEvents:UIControlEventTouchUpInside];
              [btnBg addSubview:self.scanBtn];

              self.stopBtn = [UIButton buttonWithType:UIButtonTypeSystem];
              [self.stopBtn setTitle:@"⏹ 停止" forState:UIControlStateNormal];
              [self.stopBtn setTitleColor:[UIColor whiteColor] forState:UIControlStateNormal];
              self.stopBtn.backgroundColor = [UIColor colorWithRed:0.8 green:0.2 blue:0.2 alpha:1];
              self.stopBtn.layer.cornerRadius = 6;
              self.stopBtn.frame = CGRectMake(72, 6, 50, 34);
              self.stopBtn.enabled = NO;
              [self.stopBtn addTarget:self action:@selector(stopScan) forControlEvents:UIControlEventTouchUpInside];
              [btnBg addSubview:self.stopBtn];

              self.importBtn = [UIButton buttonWithType:UIButtonTypeSystem];
              [self.importBtn setTitle:@"📥 导入" forState:UIControlStateNormal];
              [self.importBtn setTitleColor:[UIColor whiteColor] forState:UIControlStateNormal];
              self.importBtn.backgroundColor = [UIColor colorWithRed:0.2 green:0.7 blue:0.3 alpha:1];
              self.importBtn.layer.cornerRadius = 6;
              self.importBtn.frame = CGRectMake(128, 6, 50, 34);
              [self.importBtn addTarget:self action:@selector(importIPs) forControlEvents:UIControlEventTouchUpInside];
              [btnBg addSubview:self.importBtn];

              self.copyBtn = [UIButton buttonWithType:UIButtonTypeSystem];
              [self.copyBtn setTitle:@"📋 复制" forState:UIControlStateNormal];
              [self.copyBtn setTitleColor:[UIColor whiteColor] forState:UIControlStateNormal];
              self.copyBtn.backgroundColor = [UIColor colorWithRed:0.5 green:0.3 blue:0.8 alpha:1];
              self.copyBtn.layer.cornerRadius = 6;
              self.copyBtn.frame = CGRectMake(184, 6, 50, 34);
              [self.copyBtn addTarget:self action:@selector(copyResults) forControlEvents:UIControlEventTouchUpInside];
              [btnBg addSubview:self.copyBtn];

              self.clearBtn = [UIButton buttonWithType:UIButtonTypeSystem];
              [self.clearBtn setTitle:@"🗑 清空" forState:UIControlStateNormal];
              [self.clearBtn setTitleColor:[UIColor whiteColor] forState:UIControlStateNormal];
              self.clearBtn.backgroundColor = [UIColor colorWithRed:0.4 green:0.4 blue:0.4 alpha:1];
              self.clearBtn.layer.cornerRadius = 6;
              self.clearBtn.frame = CGRectMake(240, 6, 50, 34);
              [self.clearBtn addTarget:self action:@selector(clearAll) forControlEvents:UIControlEventTouchUpInside];
              [btnBg addSubview:self.clearBtn];

              y += 54;

              // ---- 列表 ----
              UIView *tableBg = [[UIView alloc] init];
              tableBg.backgroundColor = [UIColor colorWithRed:0.10 green:0.13 blue:0.22 alpha:1];
              tableBg.layer.cornerRadius = 8;
              tableBg.frame = CGRectMake(12, y, w, 200);
              [content addSubview:tableBg];

              UILabel *tableLabel = [[UILabel alloc] init];
              tableLabel.text = @"📋 可用IP";
              tableLabel.textColor = [UIColor whiteColor];
              tableLabel.font = [UIFont systemFontOfSize:13];
              tableLabel.frame = CGRectMake(12, 6, 200, 20);
              [tableBg addSubview:tableLabel];

              self.tableView = [[UITableView alloc] init];
              self.tableView.backgroundColor = [UIColor clearColor];
              self.tableView.delegate = self;
              self.tableView.dataSource = self;
              self.tableView.separatorStyle = UITableViewCellSeparatorStyleNone;
              self.tableView.frame = CGRectMake(0, 30, tableBg.bounds.size.width, 170);
              [self.tableView registerClass:[UITableViewCell class] forCellReuseIdentifier:@"Cell"];
              [tableBg addSubview:self.tableView];

              y += 208;

              // ---- 输出 ----
              UIView *outBg = [[UIView alloc] init];
              outBg.backgroundColor = [UIColor colorWithRed:0.10 green:0.13 blue:0.22 alpha:1];
              outBg.layer.cornerRadius = 8;
              outBg.frame = CGRectMake(12, y, w, 80);
              [content addSubview:outBg];

              UILabel *outLabel = [[UILabel alloc] init];
              outLabel.text = @"📝 输出";
              outLabel.textColor = [UIColor grayColor];
              outLabel.font = [UIFont systemFontOfSize:11];
              outLabel.frame = CGRectMake(12, 4, 200, 18);
              [outBg addSubview:outLabel];

              UITextView *outView = [[UITextView alloc] init];
              outView.backgroundColor = [UIColor colorWithRed:0.06 green:0.08 blue:0.15 alpha:1];
              outView.textColor = [UIColor grayColor];
              outView.font = [UIFont systemFontOfSize:11];
              outView.layer.cornerRadius = 4;
              outView.tag = 200;
              outView.frame = CGRectMake(8, 26, outBg.bounds.size.width - 16, 48);
              outView.text = @"等待扫描...";
              [outBg addSubview:outView];

              y += 88;

              content.frame = CGRectMake(0, 0, self.view.bounds.size.width, y + 20);
              scroll.contentSize = content.frame.size;
          }

          // ---- 检测运营商 ----
          - (void)detectISP {
              NSURL *url = [NSURL URLWithString:@"http://ip-api.com/json/?fields=isp"];
              [[[NSURLSession sharedSession] dataTaskWithURL:url completionHandler:^(NSData *data, NSURLResponse *response, NSError *error) {
                  if (data) {
                      NSDictionary *json = [NSJSONSerialization JSONObjectWithData:data options:0 error:nil];
                      if (json[@"isp"]) {
                          dispatch_async(dispatch_get_main_queue(), ^{
                              UILabel *l = [self.view viewWithTag:100];
                              l.text = [NSString stringWithFormat:@"📍 %@", json[@"isp"]];
                          });
                      }
                  }
              }] resume];
          }

          // ---- 导入 ----
          - (void)importIPs {
              [self.results removeAllObjects];
              [self.tableView reloadData];
              self.statusLabel.text = @"已导入";
              self.countLabel.text = @"总数: 0  可用: 0";
              [self.view endEditing:YES];

              UITextView *outView = [self.view viewWithTag:200];
              outView.text = @"等待扫描...";
              outView.textColor = [UIColor grayColor];
          }

          // ---- 清空 ----
          - (void)clearAll {
              [self.results removeAllObjects];
              [self.tableView reloadData];
              self.inputTV.text = @"";
              self.statusLabel.text = @"已清空";
              self.countLabel.text = @"总数: 0  可用: 0";
              UITextView *outView = [self.view viewWithTag:200];
              outView.text = @"等待扫描...";
              outView.textColor = [UIColor grayColor];
          }

          // ---- 扫描 ----
          - (void)startScan {
              NSString *text = self.inputTV.text;
              NSArray *lines = [text componentsSeparatedByString:@"\n"];
              NSMutableArray *ips = [NSMutableArray array];

              for (NSString *line in lines) {
                  NSString *trim = [line stringByTrimmingCharactersInSet:[NSCharacterSet whitespaceCharacterSet]];
                  if (trim.length == 0) continue;
                  NSArray *parts = [trim componentsSeparatedByString:@":"];
                  NSString *ip = parts.firstObject;
                  NSInteger port = 443;
                  if (parts.count > 1) port = [parts[1] integerValue];
                  if (port == 0) port = 443;
                  if ([ip containsString:@"."]) {
                      IPInfo *info = [[IPInfo alloc] init];
                      info.ip = ip;
                      info.port = port;
                      [ips addObject:info];
                  }
              }

              if (ips.count == 0) {
                  self.statusLabel.text = @"请导入有效IP";
                  return;
              }

              [self.results removeAllObjects];
              self.isScanning = YES;
              self.scanBtn.enabled = NO;
              self.stopBtn.enabled = YES;
              self.statusLabel.text = @"扫描中...";

              NSMutableArray *allResults = [NSMutableArray array];
              NSLock *lock = [[NSLock alloc] init];
              dispatch_group_t group = dispatch_group_create();
              dispatch_semaphore_t sem = dispatch_semaphore_create(50);

              for (IPInfo *info in ips) {
                  dispatch_group_enter(group);
                  dispatch_semaphore_wait(sem, DISPATCH_TIME_FOREVER);
                  dispatch_async(dispatch_get_global_queue(DISPATCH_QUEUE_PRIORITY_HIGH, 0), ^{
                      [self checkIP:info completion:^(IPInfo *result) {
                          [lock lock];
                          if (result) [allResults addObject:result];
                          [lock unlock];
                          dispatch_semaphore_signal(sem);
                          dispatch_group_leave(group);

                          dispatch_async(dispatch_get_main_queue(), ^{
                              self.results = [allResults mutableCopy];
                              [self.tableView reloadData];
                              self.countLabel.text = [NSString stringWithFormat:@"总数: %lu  可用: %lu", (unsigned long)ips.count, (unsigned long)self.results.count];
                              self.statusLabel.text = [NSString stringWithFormat:@"扫描中... %lu 个", (unsigned long)self.results.count];
                          });
                      }];
                  });
              }

              dispatch_group_notify(group, dispatch_get_main_queue(), ^{
                  self.isScanning = NO;
                  self.scanBtn.enabled = YES;
                  self.stopBtn.enabled = NO;
                  self.statusLabel.text = [NSString stringWithFormat:@"✅ 完成！共 %lu 个", (unsigned long)self.results.count];
                  [self updateOutput];
              });
          }

          // ---- 检查IP ----
          - (void)checkIP:(IPInfo *)info completion:(void(^)(IPInfo *))completion {
              NSString *urlStr = [NSString stringWithFormat:@"https://%@:%ld/cdn-cgi/trace", info.ip, (long)info.port];
              NSURL *url = [NSURL URLWithString:urlStr];
              NSDate *start = [NSDate date];

              NSURLSessionConfiguration *cfg = [NSURLSessionConfiguration defaultSessionConfiguration];
              cfg.timeoutIntervalForRequest = 3;
              NSURLSession *session = [NSURLSession sessionWithConfiguration:cfg];

              [[session dataTaskWithURL:url completionHandler:^(NSData *data, NSURLResponse *response, NSError *error) {
                  if (data) {
                      NSHTTPURLResponse *http = (NSHTTPURLResponse *)response;
                      if (http.statusCode == 200) {
                          double delay = [[NSDate date] timeIntervalSinceDate:start] * 1000;
                          NSString *text = [[NSString alloc] initWithData:data encoding:NSUTF8StringEncoding];
                          NSString *colo = @"";
                          for (NSString *line in [text componentsSeparatedByString:@"\n"]) {
                              if ([line hasPrefix:@"colo="]) {
                                  colo = [line substringFromIndex:5];
                                  break;
                              }
                          }

                          IPInfo *result = [[IPInfo alloc] init];
                          result.ip = info.ip;
                          result.port = info.port;
                          result.delay = delay;
                          result.colo = colo;

                          NSString *locUrl = [NSString stringWithFormat:@"http://ip-api.com/json/%@?fields=country", info.ip];
                          [[[NSURLSession sharedSession] dataTaskWithURL:[NSURL URLWithString:locUrl] completionHandler:^(NSData *d, NSURLResponse *r, NSError *e) {
                              if (d) {
                                  NSDictionary *json = [NSJSONSerialization JSONObjectWithData:d options:0 error:nil];
                                  result.country = json[@"country"] ?: @"";
                              }
                              completion(result);
                          }] resume];
                          return;
                      }
                  }
                  completion(nil);
              }] resume];
          }

          // ---- 停止 ----
          - (void)stopScan {
              self.isScanning = NO;
              self.scanBtn.enabled = YES;
              self.stopBtn.enabled = NO;
              self.statusLabel.text = @"已停止";
          }

          // ---- 输出 ----
          - (void)updateOutput {
              NSMutableString *outText = [NSMutableString string];
              for (IPInfo *info in self.results) {
                  [outText appendFormat:@"%@:%ld  [%.0fms]  %@  %@\n", info.ip, (long)info.port, info.delay, info.country, info.colo];
              }
              UITextView *outView = [self.view viewWithTag:200];
              outView.text = outText.length > 0 ? outText : @"暂无数据";
              outView.textColor = outText.length > 0 ? [UIColor whiteColor] : [UIColor grayColor];
          }

          // ---- 复制 ----
          - (void)copyResults {
              NSMutableString *text = [NSMutableString string];
              for (IPInfo *info in self.results) {
                  [text appendFormat:@"%@:%ld\n", info.ip, (long)info.port];
              }
              if (text.length > 0) {
                  [UIPasteboard generalPasteboard].string = text;
                  self.statusLabel.text = @"✅ 已复制";
              }
          }

          // ---- TableView ----
          - (NSInteger)tableView:(UITableView *)tableView numberOfRowsInSection:(NSInteger)section {
              return self.results.count;
          }

          - (UITableViewCell *)tableView:(UITableView *)tableView cellForRowAtIndexPath:(NSIndexPath *)indexPath {
              UITableViewCell *cell = [tableView dequeueReusableCellWithIdentifier:@"Cell" forIndexPath:indexPath];
              cell.backgroundColor = [UIColor clearColor];
              cell.textLabel.textColor = [UIColor whiteColor];
              cell.textLabel.font = [UIFont systemFontOfSize:11];

              IPInfo *info = self.results[indexPath.row];
              cell.textLabel.text = [NSString stringWithFormat:@"%@:%ld  [%.0fms]  %@  %@", info.ip, (long)info.port, info.delay, info.country, info.colo];
              return cell;
          }

          - (CGFloat)tableView:(UITableView *)tableView heightForRowAtIndexPath:(NSIndexPath *)indexPath {
              return 30;
          }

          @end

          // ===== App入口 =====
          @interface AppDelegate : UIResponder <UIApplicationDelegate>
          @property (strong, nonatomic) UIWindow *window;
          @end

          @implementation AppDelegate

          - (BOOL)application:(UIApplication *)application didFinishLaunchingWithOptions:(NSDictionary *)launchOptions {
              self.window = [[UIWindow alloc] initWithFrame:[UIScreen mainScreen].bounds];
              self.window.rootViewController = [[UINavigationController alloc] initWithRootViewController:[[ViewController alloc] init]];
              [self.window makeKeyAndVisible];
              return YES;
          }

          @end

          int main(int argc, char * argv[]) {
              @autoreleasepool {
                  return UIApplicationMain(argc, argv, nil, @"AppDelegate");
              }
          }
          EOF

          cat > CloudFlareScan/Info.plist << 'EOF'
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
              <key>CFBundleDevelopmentRegion</key>
              <string>zh_CN</string>
              <key>CFBundleExecutable</key>
              <string>CloudFlareScan</string>
              <key>CFBundleIdentifier</key>
              <string>com.cloudflarescan.ios</string>
              <key>CFBundleInfoDictionaryVersion</key>
              <string>6.0</string>
              <key>CFBundleName</key>
              <string>优选IP</string>
              <key>CFBundlePackageType</key>
              <string>APPL</string>
              <key>CFBundleShortVersionString</key>
              <string>1.0</string>
              <key>CFBundleVersion</key>
              <string>1</string>
              <key>LSRequiresIPhoneOS</key>
              <true/>
              <key>NSAppTransportSecurity</key>
              <dict>
                  <key>NSAllowsArbitraryLoads</key>
                  <true/>
              </dict>
              <key>UILaunchScreen</key>
              <dict/>
              <key>UIRequiredDeviceCapabilities</key>
              <array>
                  <string>armv7</string>
              </array>
              <key>UISupportedInterfaceOrientations</key>
              <array>
                  <string>UIInterfaceOrientationPortrait</string>
              </array>
          </dict>
          </plist>
          EOF

      - name: 创建项目文件
        run: |
          cat > CloudFlareScan.xcodeproj/project.pbxproj << 'EOF'
          // !$*UTF8*$!
          {
              archiveVersion = 1;
              classes = {};
              objectVersion = 56;
              objects = {
                  C100000001 /* main.m in Sources */ = {isa = PBXBuildFile; fileRef = C100000002 /* main.m */; };
                  C100000002 /* main.m */ = {isa = PBXFileReference; lastKnownFileType = sourcecode.c.objc; path = main.m; sourceTree = "<group>"; };
                  C100000003 /* Info.plist */ = {isa = PBXFileReference; lastKnownFileType = text.plist.xml; path = Info.plist; sourceTree = "<group>"; };
                  C100000004 /* CloudFlareScan.app */ = {isa = PBXFileReference; explicitFileType = wrapper.application; includeInIndex = 0; path = CloudFlareScan.app; sourceTree = BUILT_PRODUCTS_DIR; };
                  C100000005 = {isa = PBXGroup; children = (C100000006, C100000007,); sourceTree = "<group>"; };
                  C100000006 /* CloudFlareScan */ = {isa = PBXGroup; children = (C100000002, C100000003,); path = CloudFlareScan; sourceTree = "<group>"; };
                  C100000007 /* Products */ = {isa = PBXGroup; children = (C100000004,); name = Products; sourceTree = "<group>"; };
                  C100000008 /* CloudFlareScan */ = {isa = PBXNativeTarget; buildConfigurationList = C100000009; buildPhases = (C100000010, C100000011,); buildRules = (); dependencies = (); name = CloudFlareScan; productName = CloudFlareScan; productReference = C100000004; productType = "com.apple.product-type.application"; };
                  C100000012 /* Project object */ = {isa = PBXProject; attributes = {LastSwiftUpdateCheck = 1500; LastUpgradeCheck = 1500; TargetAttributes = {C100000008 = {CreatedOnToolsVersion = 15.0;};};}; buildConfigurationList = C100000013; compatibilityVersion = "Xcode 14.0"; developmentRegion = en; hasScannedForEncodings = 0; knownRegions = (en, Base,); mainGroup = C100000005; productRefGroup = C100000007; projectDirPath = ""; projectRoot = ""; targets = (C100000008,); };
                  C100000011 /* Resources */ = {isa = PBXResourcesBuildPhase; buildActionMask = 2147483647; files = (); runOnlyForDeploymentPostprocessing = 0; };
                  C100000010 /* Sources */ = {isa = PBXSourcesBuildPhase; buildActionMask = 2147483647; files = (C100000001,); runOnlyForDeploymentPostprocessing = 0; };
                  C100000014 /* Debug */ = {isa = XCBuildConfiguration; buildSettings = {IPHONEOS_DEPLOYMENT_TARGET = 15.0; SDKROOT = iphoneos; TARGETED_DEVICE_FAMILY = "1,2"; CODE_SIGN_IDENTITY = ""; CODE_SIGNING_ALLOWED = NO; CODE_SIGNING_REQUIRED = NO;}; name = Debug; };
                  C100000015 /* Release */ = {isa = XCBuildConfiguration; buildSettings = {IPHONEOS_DEPLOYMENT_TARGET = 15.0; SDKROOT = iphoneos; TARGETED_DEVICE_FAMILY = "1,2"; CODE_SIGN_IDENTITY = ""; CODE_SIGNING_ALLOWED = NO; CODE_SIGNING_REQUIRED = NO;}; name = Release; };
                  C100000016 /* Debug */ = {isa = XCBuildConfiguration; buildSettings = {CODE_SIGN_STYLE = Manual; CURRENT_PROJECT_VERSION = 1; DEVELOPMENT_TEAM = ""; INFOPLIST_FILE = CloudFlareScan/Info.plist; MARKETING_VERSION = 1.0; PRODUCT_BUNDLE_IDENTIFIER = com.cloudflarescan.ios; PRODUCT_NAME = CloudFlareScan; CODE_SIGN_IDENTITY = ""; CODE_SIGNING_ALLOWED = NO; CODE_SIGNING_REQUIRED = NO;}; name = Debug; };
                  C100000017 /* Release */ = {isa = XCBuildConfiguration; buildSettings = {CODE_SIGN_STYLE = Manual; CURRENT_PROJECT_VERSION = 1; DEVELOPMENT_TEAM = ""; INFOPLIST_FILE = CloudFlareScan/Info.plist; MARKETING_VERSION = 1.0; PRODUCT_BUNDLE_IDENTIFIER = com.cloudflarescan.ios; PRODUCT_NAME = CloudFlareScan; CODE_SIGN_IDENTITY = ""; CODE_SIGNING_ALLOWED = NO; CODE_SIGNING_REQUIRED = NO;}; name = Release; };
                  C100000009 = {isa = XCConfigurationList; buildConfigurations = (C100000016, C100000017,); defaultConfigurationIsVisible = 0; defaultConfigurationName = Release; };
                  C100000013 = {isa = XCConfigurationList; buildConfigurations = (C100000014, C100000015,); defaultConfigurationIsVisible = 0; defaultConfigurationName = Release; };
              };
              rootObject = C100000012;
          }
          EOF

      - name: 编译
        run: |
          xcodebuild -project CloudFlareScan.xcodeproj \
            -scheme CloudFlareScan \
            -configuration Release \
            -derivedDataPath build \
            -sdk iphoneos \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO

      - name: 打包
        run: |
          APP_PATH=$(find build -name "*.app" -type d | head -1)
          if [ -z "$APP_PATH" ]; then
            echo "未找到 .app"
            exit 1
          fi
          mkdir -p Payload
          cp -R "$APP_PATH" Payload/
          zip -r CloudFlareScan.ipa Payload
          cp CloudFlareScan.ipa CloudFlareScan.tipa

      - name: 上传
        uses: actions/upload-artifact@v4
        with:
          name: CloudFlareScan-TrollStore
          path: |
            *.ipa
            *.tipa
